Здравствуйте,

создал новый проект, так как shopproject был уже на уроках реализован. Реализовал все три метода работы с базами данных (DriverManager, JdbcTemplate, JpaRepository), единственное в JpaRepository получилось реализовать только findAll(), findById(), save() и delete().

Для реализации трёх методик работ с базами данных создал 3 пакета:
old для работы с DriverManager
middle для работы с JdbcTemplate
last для работы с JpaRepository

Проект назвал изначально Bibliothek, так как в пакете last хотел попробовать соединить три entity (User, Book, Author) в три таблицы, что получается сделать, но реализовать вызовы между ними не получилось, так как из-за primary key при заполнении таблиц выпадали ошибки и т. д. поэтому решил бросить затею с тремя взаимосвязанными таблицами.

В проекте добавил документ old_user.csv, в котором находятся данные таблицы, которые использовал в своём проекте.

Pull Request на ваш репоситорий сделать не могу, поэтому решил переслать в качестве ссылки на собственный репоситорий. 